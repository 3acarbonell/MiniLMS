Install MiniLMS

```bash
  # add and install dependencies
  pip install requirements.txt

  # setup users, courses and contents
  build.sh

  # server on 127.0.0.1:8000
  python manage.py runserver
```
    